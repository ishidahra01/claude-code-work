{
  "name": "claude-code-work",
  "version": "0.1.0",
  "description": "Slide-generation workflow powered by Marp, Claude Code, Skills, and Git",
  "private": true,
  "scripts": {
    "preview": "marp slides/deck.md --watch",
    "build:html": "marp slides/deck.md --html --output dist/deck.html",
    "build:pdf": "marp slides/deck.md --pdf --output dist/deck.pdf",
    "build:pptx": "marp slides/deck.md --pptx --output dist/deck.pptx",
    "build:all": "npm run build:html && npm run build:pdf && npm run build:pptx"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0"
  }
}
